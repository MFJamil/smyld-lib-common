import Vue from 'vue';

export class VueLoggerPlugin{
    public install(vue:Vue, opts?:any){   
        console.log('Installing the CommentsOverlay plugin!')
        
        // Fun will happen here
    }
}